import React from 'react';

const OrderNote = () => (
  <div>
    <hr />
    <label>Note: Click on column header to arrange in ascending and decending order</label><br />
    <hr />
  </div>
);

export default OrderNote;
